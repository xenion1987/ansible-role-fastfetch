[project]
name = "ansible-role-fastfetch"
version = "0.1.0"
description = "Install the latest fastfetch version from GitHub"
readme = "README.md"
requires-python = ">=3.12,<3.15"

[dependency-groups]
dev = [
    "ansible-core>=2.18",
    "ansible-dev-tools",
    "ansible-doctor",
    "prek",
    "molecule",
    "molecule-plugins[podman]",
]
openstack = [
    "openstacksdk",
    "molecule-plugins[openstack]",
]
